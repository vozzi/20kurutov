
function integer(a,b) {

  if (a > 0 && b > 0 && a > b) {
    document.write("The larger num is " + a );
  } else{ (a > 0 && b > 0 && a < b)
    document.write("The larger num is " + b);
  } 
}

integer(2, 5);